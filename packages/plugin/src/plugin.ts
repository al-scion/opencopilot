import { type Plugin, tool } from "@opencode-ai/plugin";

export const OpencopilotPlugin: Plugin = async (ctx) => {
	console.log("Opencopilot Plugin Loaded Successfully!");

	return {};
};

export default OpencopilotPlugin;
